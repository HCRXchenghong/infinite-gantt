# 复制本文件为 .env 后按需修改；.env 已被 gitignore，不会提交
# server.js 会自动读取 .env，无需 --env-file 参数

# 后台管理账号（必填）
# 未配置时服务会随机生成一个临时密码并打印到控制台，仅供当次启动使用
ADMIN_USER=your_admin_username
ADMIN_PASS=your_strong_password

# 服务端口，默认 3000
PORT=3000
